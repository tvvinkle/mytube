extends layout/main

block content
    h3 Delete Video
    p=theVideo.title
    div.form-container
        form(action=routes.videoDelete(theVideo.id), method="post")
            input(name="title", type="text", placeholder="Title", value=theVideo.title)
            input(name="description", type="text", placeholder="Description", value=theVideo.description) 
            input(type="submit", value="Delete Video")
    a.form-container__link(href=routes.videoEdit(theVideo.id)) Cancle
